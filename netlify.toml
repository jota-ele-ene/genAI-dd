[build]
  publish = "public"
  functions = "netlify/functions"

# Opcional: ruta “bonita” /api/ask-bot en lugar del path interno
[[redirects]]
  from = "/api/ask-bot"
  to = "/.netlify/functions/ask-bot"
  status = 200

[build.environment]
  SECRETS_SCAN_OMIT_PATHS = "/public/css"
